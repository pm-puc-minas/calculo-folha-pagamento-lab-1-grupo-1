package com.payroll.service;

// Deprecated - using ReportsService.java
// ...
@org.springframework.stereotype.Service
public class ReportService {
    // Stub to satisfy injection if any legacy code still references it, though we should remove references.
    // If it's referenced by ReportController (which is now empty), it won't be called.
    
    public void dummy() {}
}
